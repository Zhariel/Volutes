pub struct Math{}

impl Math{

    // break down concave objects so we can apply delaunay triangulation
    pub fn convex_decomposition(vertices: Vec<f32>, indices: Vec<i32>){

    }

    pub fn triangulate(vertices: Vec<f32>, indices: Vec<i32>){

    }
}